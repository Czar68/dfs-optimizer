# Production build: IONOS document root is /dfs/, so dist/data/* is served at /data/* (not /dfs/data/*)
# App must request /data/* so server resolves to /dfs/data/* on disk → 200
VITE_DATA_BASE=/data
