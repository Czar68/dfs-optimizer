/**
 * Phase 22 — Canonical sample artifact contract constants (no Node builtins; safe for browser bundles).
 * Phase 20 envelope JSON uses these values.
 */

export const CANONICAL_SAMPLE_SCHEMA_VERSION = 1 as const;
export const PHASE20_SAMPLE_CONTRACT_ID = "phase20_canonical_sample_v1" as const;
