// Environment loading for optimizer
import { config } from 'dotenv';

// Load environment variables
config();

export function loadEnv(): void {
  // Environment loading handled by dotenv config()
}
