# Archive (non-runtime)

Files moved here are **not** part of the optimizer pipeline, `verify:canonical`, or active entrypoints. They are kept for historical reference or optional manual CLI use.

**Phase 17V:** `validation/tweak_backtest.ts` — offline perf-tracker tweak comparison (run from repo root: `npx ts-node tools/archive/validation/tweak_backtest.ts`).
